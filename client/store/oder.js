export const state = () => ({
// State
cartItems: 5,
});

export const getters = {
// Getters
  G_CART: state => state.cartItems
};

export const mutations = {
// Mutations
};

export const actions = {
// Actions
};
